{% extends 'base.html.twig' %}

{% block body %}

        <div class="row">
            <div class="col s12 m12 white-custom center marg-bot">
                <h1>Formulaire de <span class="blue-text">contact</span></h1>
                {{ form_start(form) }}
                <div class="col m6 offset-m3">
                    <div class="row">
                        <div class="col m12 black-text marg-more">
                            <label for="form_email">Email</label>
                            {{ form_widget(form.email) }}
                        </div>
                        <div class="col m12 marg-more">
                            <label for="form_message">Message</label>
                            {{ form_widget(form.message) }}
                        </div>
                        <div class="col m12 marg-more">
                            {{ form_widget(form.Envoyer) }}
                        </div>
                    </div>
                {{ form_end(form) }}
                </div>
            </div>
        </div>
<script>
    $(document).ready(function() {
        M.updateTextFields();
    });
</script>
{% endblock %}